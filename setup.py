from setuptools import setup

setup(
    name="clearsolutions",
    author="Christian Geier",
    description="Clear jupyter cells tagged with `solution`",
    packages=["clearsolutions"],
    version=0.1,
    license="Expat",
)
